name: smoke_2agent_t3
description: |
  Smoke test for v15 two-stage belief then trade loop.
market:
  slug: will-tesla-launch-a-driverless-robotaxi-service-by-october-31
  asof: market_open
agent:
  population: archetype
  n_agents: 2
  seed: 0
  belief_update_enabled: true
environment:
  observer: quote_only
  seeder: from_clob_history
  fees_override_bps: 0
llm:
  model: null
  temperature: 1.0
  timeout_s: 120.0
  retry: {max_attempts: 3, backoff_base_s: 2.0}
  concurrency: 2
  thinking: false
experiment:
  n_ticks_override: 3
  checkpoint_enabled: true
  checkpoint_interval_ticks: 5
  checkpoint_compact_char_budget: 60000
  checkpoint_recent_ticks: 5
output:
  dual_write_clickhouse: false
  parquet_compression: zstd
  output_dir: output/v15/smoke
