本 tick 的决策阶段 1/2 已完成：
  P(YES) = {{ "%.3f"|format(yes_prob) }}
  Confidence = {{ "%.2f"|format(confidence) }}
  Rationale = "{{ rationale }}"

决策阶段 2/2：基于这个信念，判断现在是否执行一个交易相关动作。你可以调用且只调用一个可用交易工具，也可以不调用工具表示 HOLD。请根据你的 persona、持仓、当前盘口和风险自主判断。
