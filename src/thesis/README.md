# `src/thesis/` — Step 6: Paper helpers

Stage D (next) populates this with `tables.py` (markdown + LaTeX
table generators consumed by `docs/PAPER.md`) and any
citation-management helpers.

Currently a stub. See `docs/PAPER.md` for the thesis paper outline.
