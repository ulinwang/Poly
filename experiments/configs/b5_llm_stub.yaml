name: b5_llm_stub
description: "v13 B5 \u2014 LLM-backend probe. STUB ONLY. Requires Anthropic key wiring\
  \ (TODO). Do not run."
market:
  slug: will-roman-roy-be-ceo-at-the-end-of-succession-s4
  asof: market_open
agent:
  population: archetype
  n_agents: 30
  seed: 0
  belief_update_enabled: false
environment:
  observer: quote_only
  seeder: from_clob_history
llm:
  model: claude-3-7-sonnet
  temperature: 1.0
  timeout_s: 120.0
  retry:
    max_attempts: 3
    backoff_base_s: 2.0
  concurrency: null
output:
  dual_write_clickhouse: false
  parquet_compression: zstd
  output_dir: output/v13/b5
_stub_todo: requires Anthropic key + decision.llm provider dispatch; not yet wired
