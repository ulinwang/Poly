# Python-generated files
__pycache__/
*.py[oc]
build/
dist/
wheels/
*.egg-info

# Virtual environments
.venv

# Local secrets / OS
.env
.DS_Store
# v8: data/ is a source-code package; only runtime artifacts ignored
/data/priors_*.json
/data/wallet_personas.json
/data/personas_*.json
/data/runs/

# Experiment result trees — not committed; regenerable by re-running
# the experiment suites. Includes per-run parquet, llm_calls.jsonl,
# per-run figures, summaries, comparison outputs and run logs.
/output/
/output_v13/
/output_concurrent/
/comparison/
/comparison_final/
/comparison_v101/

# v7-era artifact directories (kept for backwards compat)
/figures/
/tables/

.claude/

# v13 clustering feature intermediates — large, regenerable from ClickHouse
data/clustering/wallet_features_*.parquet

# Large publication TIFFs — regenerable from scripts/thesis_*.py
docs/v13/figures/*.tiff

# Frontend node_modules + dist (build artifacts)
webapp/frontend/node_modules/
webapp/frontend/dist/

# __pycache__ everywhere
__pycache__/

# Thesis / research artifacts — decoupled from code repo
# See THESIS_DECOUPLING.md for details
docs/
data/clustering/
*.parquet
*.pdf
*.png
*.tiff
*.svg

# Backend TS build artifacts
backend/dist/
backend/node_modules/
backend/package-lock.json
backend/data/

# Local dev artifacts
.mplconfig/
tmp/
